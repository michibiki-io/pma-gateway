apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "pma-gateway.configMapName" . }}
  labels:
    {{- include "pma-gateway.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.config }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
